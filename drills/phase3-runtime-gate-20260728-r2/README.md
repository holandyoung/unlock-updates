# Unlock Catalog Phase 3 runtime gate r2

This isolated feed is the production-capability-validated rerun of the Catalog
runtime verification drill. `current/manifest.json` advances through v1, v2,
and v3; `archive/` preserves the immutable signed inputs.

The feed contains synthetic metadata and artifacts only. Its signing keys are
one-time drill keys and are not an official release trust root.
