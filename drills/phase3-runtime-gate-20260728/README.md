# Unlock Catalog Phase 3 runtime gate

This isolated feed records the signed production drill for Catalog runtime
verification. `current/manifest.json` is the source endpoint advanced during
the drill; `archive/` preserves the immutable v1, v2, and v3 inputs.

The feed contains synthetic metadata and artifacts only. Its signing keys are
one-time drill keys and are not an official release trust root.
